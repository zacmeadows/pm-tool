<center>
  <h1>All Projects</h1>

  <% @projects.each do |project| %> 
    <h2><%= link_to project.title, project_path(project) %></h2>
    <p><%= project.description %></p>
    <hr>
  <% end %>
</center>